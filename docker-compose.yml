version: '3.9'

services:
  app:
    build:
      target: app-prod
      dockerfile: Dockerfile
    image: webdevops/php-nginx:8.2
    ports:
      - "8080:80"
    volumes:
      - ./public:/app/public:cached
      - ./src:/app/src:cached   # optional, if you have other PHP code
    environment:
      - WEB_DOCUMENT_ROOT=/app/public
    restart: unless-stopped